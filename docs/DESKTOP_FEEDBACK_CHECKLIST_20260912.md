# 本任务用户反馈逐项核对

以本任务用户消息为依据；代码、安装和真实交互分别验收。未勾选不得称完成。

## 正在交付的修复

- [ ] 搜索栏与历史按钮胶囊；历史宽度由 74 改为 148，图标文字居中。代码已改，待最新包实看。
- [ ] 搜索直接显示所有结果，不分页。现有代码和回归覆盖，待全分类实测。
- [ ] 搜索首次点击：立即失效旧请求、单一分类入口。已安装前一版，视频分类单击实测通过，其他分类待验。
- [ ] 搜索超时与延迟：图片可视区加载、并发 2、查询合并；当前索引 7297 条、过期 0。仍缺全链路计时，不能称全面流畅。
- [ ] 搜索附件长按与右键：轻微抖动容忍、右键接管；行为测试通过。正式应用音频 15:28 卡右键实测显示预览/定位/删除引用；其余类型和真实长按待验。
- [ ] 删除引用二次确认与定位回对话：音频卡二次确认及取消已实测，未删除数据；定位仍待实测。
- [ ] 普通消息图片无灰底、按原比例收窄：此前仅修改生成图片图库，普通图片漏改；本次已补，待安装实看。
- [ ] 音频暗底与视频一致：已有样式，待同视口核对。
- [ ] 进度条前后缺口/多余白条：已有修改，待端点、中点和拖动核对。
- [ ] 视频/音频音量：视频竖条及 100→50 已原生实测；追加发现 app 本地图标表缺 audio，生成 undefined path。改为共享 icon-source 喇叭并增加真实渲染函数回归。弹层 44×128、轨道高 92、滑块 16；最新包已覆盖，正式视频实看喇叭及紧凑竖条通过。音频入口仍待实看。
- [ ] 文本附件与手机同义渲染：SharedTextPreviewDialog / inertTextPreviewBlock 对照完成，Desktop 使用安全 Markdown 和 inert 链接标签；真实 20260830-OCR.md 标题层级已原生验证。最新 inert 修复已安装，原始 HTML 仍按安全纯文本显示，未执行。其余格式待逐类对照。
- [ ] 复制后不显示绿色勾号：已安装，保留无障碍完成提示，待视觉复核。
- [ ] 复制与菜单不改变正文位置：已修重绘/焦点/滚动恢复；仍待真实长文、复制失败、菜单关闭的完整回归。
- [ ] 图片每次只切一张：已修惯性锁及过期读取；行为测试通过，仍待真实连续触控板验收。
- [ ] 所有主题色焦点线框去除：已安装 CSS；仍待浅/深主题及鼠标/键盘焦点视觉核对。
- [ ] 本次回答信息：删“本机回答审计”和底部安全说明，名称和值双列对齐。已安装，待实看。
- [ ] 删除“本地工作区已就绪…”常驻提示：已安装，静态回归通过。
- [ ] 恢复保护与首次同步方向布局紧凑、按钮文字居中：部分已有样式，仍需当前状态实看。
- [ ] 恢复码复制按钮与确认居中：已有代码，待真实 UI 无秘密记录验收。
- [ ] Desktop 使用手机原恢复码连接：已有代码，未进行真实恢复/上传。
- [ ] 恢复与安全卡片排版：已合并紧凑操作组，待当前账号页实看。
- [ ] Android 同步恢复与安全功能：未完成；缺云端文档发现及轮换流程，不能仅复制 Desktop 按钮。不得宣称跨端闭环。

## 数据门禁

- 所有覆盖均保留旧应用备份，签名 Team 保持 457B263L9J。
- 替换应用前后核对数据库 hash；此前各次覆盖保持一致。
- 不上传对话、不更换真实恢复码、不删除真实引用、不安装 Android 测试包。

## 2026-09-12 当前收口 / 下一项

- 用户最新图二覆盖旧菜单设计：附件操作不再大居中信息弹窗，改为指针/长按点附近宽 280 的小弹层，只留文件名、快速定位、删除；单击卡片继续预览，二次删除确认不变。`search-attachment-menu.mjs` 统一渲染，CSS clamp 限制窗口边缘。
- 搜索占锁修复已实现：图片读取/校验/解码/缩放从 AppState store mutex 转入 paths worker，保留文件 hash 与尺寸校验。Rust 真正图像读写/原图/缺失文件及持锁时 paths worker 可完成回归通过；全量 Node 通过。真实全分类耗时仍待验证，不能据此称全部性能问题消失。

- 用户追加音量视觉统一：弹层与播放栏共用 `--playback-control-surface` / `--playback-control-blur`，同 999px 胶囊；删除独立边框/重阴影，居中对齐喇叭按钮。保留 44×128 紧凑尺寸及竖条。定向共享样式/图标/展开行为 3/3，全量 Node 通过（`/tmp/nanfeng-volume-unified-tests.log`）。

- 最新覆盖备份：`/Users/nanzhufeng/Applications/nanfeng-volume-backup-rENmA2/南枫 AI Desktop.app`；当前正式路径不变。
- 本次替换前后 SQLite SHA256 均为 `50b834a56143faf7da36622cdcd35adb336d8861816f1103d85ddae9820393ea`。启动后的正常状态写入不要求仍等于旧哈希。
- Node 全量通过（日志 `/tmp/nanfeng-volume-compact-tests.log`）；macOS build / strict codesign 通过。没有 Android 安装或真实云测试。
- 图标审计额外发现 `app.mjs` 的来源列表使用不存在的本地 `icons.globe`（约 1539 行），尚未修复，下一小增量切到共享图标并补缺失引用门禁。
- 全功能手机代码差异审计尚未完成，不能给出最终遗漏总数。先按本清单收完附件入口，再核对 Android 设置/模型/记忆/知识/导入导出/同步所有 UI 与 domain owner；不要把历史视觉矩阵的“已闭环”当当前真实链路证明。
- 上下文闸门返回 HANDOFF；不在当前高占用上下文继续开启新模块。交接路由由本表保留，禁止漏掉 Android 恢复与安全、搜索性能计时等未完成项。
