# Android 与 Desktop 用户入口审计（2026-08-16）

> **2026-08-26 当前读取门（优先于全文）：** 用户已要求删除“设置 → 功能审阅”入口。本文继续保留跨端能力、owner、风险和历史验收的审计价值，但不再授权恢复该用户可见页面。后文所有“功能审阅”字样均为历史登记位置；当前产品入口、页面层级和可见文案只读 [Android 当前会话界面合同](ANDROID_CONVERSATION_UI_CURRENT_CONTRACT.md) 与 [Android 当前设置界面合同](ANDROID_SETTINGS_UI_CURRENT_CONTRACT.md)，普通聊天的个性化、Memory 与资料库上下文另只读 [Android 当前运行时上下文合同](ANDROID_RUNTIME_CONTEXT_CURRENT_CONTRACT.md)，工程去留记录写入对应合同或 `PRODUCT_FEEDBACK_DECISION_LEDGER.md`。

## 任务契约

- 目标：让已经实现且适合普通用户的本地功能，从 Android、Desktop 或其设置中心有可定位入口；不把凭据、Provider 诊断、fixture、离线 Eval 或工程控制带入正常聊天/设置。
- 范围：当前 Android Compose 与 macOS Desktop chat-first 壳；Compare 的可见入口及其不外发边界一并审计。
- 不改：已人工确认的 Drawer、消息区、普通发送、普通 `自动` 语义、既有显性按钮及核心布局关系；不读 Key、不发 HTTP、不操作 OPPO。
- 判定：`缺口` 只表示产品入口，不把尚无跨端 owner、真实服务或安全合同的能力包装成可用功能。

## 新增功能决策登记（2026-08-26 起生效；覆盖下方历史审阅规则）

- 每项面向普通用户的新功能，必须在同一改动中记录 Android／Desktop 的真实可用性、去留状态、现有入口、是否建议新增常驻按键及必要理由；不具备同等 owner 的平台必须如实标为待实现。
- 记录写入对应领域合同或 `PRODUCT_FEEDBACK_DECISION_LEDGER.md`；**不得**以登记为理由恢复“设置 → 功能审阅”页面、重复小字或伪开关。
- 默认复用当前设置层级或既有动作。若建议在聊天、Composer、会话详情或工作页新增常驻按键，必须说明位置、误触和隐私影响，并先由用户确认。
- 凭据、Provider 诊断、fixture、离线 Eval、工程控制不得进入普通设置；未具备真实 owner 的候选能力不得展示为可用配置或执行入口。

### 2026-08-30 DeepSeek 实时峰谷提示（既有入口增强）

- **Android：** 既有“日常／深度 → 选择具体模型”列表中，DeepSeek V4 Flash 与 V4 Pro 的小字使用主题色粗体显示当前低谷／高峰，并在官方 UTC 分界点自动切换；它与费用估算共用同一时间 owner，不新增开关、持久状态或网络请求。
- **Desktop：** 待实现。Desktop 没有同一 Android Compose 模型选择面，本轮不展示伪状态。
- **建议：** 只增强既有瞬时模型选择面，不新增聊天主页、Composer 或设置常驻按钮；用户能在选择前直接看到当前计费时段，又不会增加操作层级。

### 2026-08-28 本地文本预览语义排版（既有入口增强）

- **Android：** 既有本地文本附件预览复用会话安全 Markdown 投影，隐藏控制字符并呈现紧凑标题、引用、列表、强调、代码与表格；链接保持不可执行，下载／分享继续使用未修改原文件。浅色预览白色顶部延伸到状态栏并使用黑色系统图标；底部改为选择复制和原文件边界提示。
- **Desktop：** 本轮不扩展 Desktop owner，也不宣称与 Android 同步完成。
- **建议：** 只增强现有文本附件预览，不新增聊天、Composer、设置或文件页常驻按键；下载、分享和关闭位置不变。

### 2026-08-27 回答级上下文来源说明与临时聊天隔离（既有入口增强）

- **Android：** 普通 Assistant 回复的既有页脚，在本次回复存在已绑定本地上下文审计时增加一个来源图标；点击仅显示实际加入的本地资料类别、标题和使用原因，不显示资料正文、Prompt、附件、Provider 原始请求或凭据。没有额外资料时如实说明只使用本轮输入、当前对话路径和固定系统规则。临时聊天继续是独立本机恢复链：最多 24 小时，不连接模型服务，不读取记忆或资料库、不进入普通搜索、不自动标题或记忆摘要；它不显示该回答级入口。
- **Desktop：** 待实现。Desktop 没有同一 Attempt→回答绑定与临时离线 owner，不展示伪图标或“已隔离”承诺。
- **建议：** 只复用每条 Assistant 回复的既有页脚，不在聊天主页、Composer、设置或临时聊天增加常驻按钮；来源说明按需点击，避免把可能包含用户资料标题的事实长期铺在阅读面。

### 2026-08-27 会话一键上下阅读（既有入口增强）

- **Android：** 既有“到最新消息”浮动按钮改为真实边界语义：短按向下一整可见阅读屏，长按直达真实末端并恢复跟随；新增同组件的顶部“回到对话开头”浮动按钮：短按向上一整可见阅读屏，长按直达真实开头并保持关闭跟随。两者默认不常驻：手动向下／向上阅读立即显示对应方向，画面停止滚动超过 3 秒自动消失；程序自动跟随不会显示入口。到对应真实边界也会立即隐藏。顶部按钮圆心与既有菜单按钮同一行；不读取、不外发或修改任何会话内容。
- **Desktop：** 待实现。Desktop 没有同一 Compose 滚动 owner，不将 Android 修复伪装为跨端一致。
- **建议：** 用户已明确要求补齐顶部回溯入口，故仅在聊天正文的既有浮层层级增加同形按钮；不在聊天主页、Composer、会话详情或设置增加常驻按键。上下按钮均依据真实滚动边界显示，避免无效控件和误触。

### 2026-08-27 单条回复导出 Markdown（新增）

- **Android：** 已启用真实 owner。每条 Assistant 回复的既有页脚保留复制、分享和三个点；三个点弹层首项为“导出 Markdown”，第二项为“创建分支”。点击导出后仅将该条当前可见回复重建为 `.md`，通过本机 `FileProvider` 交给系统保存／分享。不会导出 Prompt、Provider 原始回包、凭据、其他消息或附件字节。
- **Desktop：** 待实现。Desktop 尚无同等单条回复 Markdown 文件导出 owner，不显示伪图标或入口。
- **建议：** 单条回复只复用每条 Assistant 回复的页脚动作区；导出与创建分支属于次要动作，统一收进三个点弹层，不在聊天主页、Composer 或设置中新增常驻入口；会话级导出另在下方限定为既有会话长按菜单，避免将两个导出范围混淆。

### 2026-08-27 会话当前路径导出 Markdown（新增）

- **Android：** 已启用真实 owner。侧栏会话长按菜单在“分享”后提供“导出 Markdown”文件下载图标；只导出该会话当前可见路径的标题、用户／Assistant 内容和显示时间，写为 `.md` 后通过本机 `FileProvider` 交给系统保存／分享。隐藏分支、Prompt、Provider 原始回包、凭据、其他会话及附件字节均不导出。
- **Desktop：** 待实现。Desktop 尚无同等会话当前路径 Markdown 导出 owner，不显示伪菜单项。
- **建议：** 只放在既有会话长按菜单，不在聊天主页、Composer 或设置中增加常驻入口；“单条回复导出”仍只在 Assistant 页脚，两个范围不混淆。

### 2026-08-27 聊天内查找命中高亮（既有入口增强）

- **Android：** 已启用真实本地展示 owner。用户在“在聊天中查找”提交关键词后，既有逐条定位不变；当前会话正文的所有可见命中以当前主题色文字和浅中性灰圆角胶囊底突出。关闭查找立即撤销高亮，不改消息、索引、选择文本或发送内容。
- **Desktop：** 待实现。Desktop 尚无同等聊天内查找与正文高亮 owner，不显示伪开关或入口。
- **建议：** 只增强现有查找面与其导航条，不增加聊天主页、Composer 或设置常驻按键；高亮以小面积胶囊提示，不用整行色带干扰阅读。

### 2026-08-27 本地会话收藏（新增）

- **Android：** 已启用真实本机 owner。会话长按菜单和左侧列表右滑操作带都在“置顶”下方提供“收藏／取消收藏”；收藏时间持久化在本机 Room，设置 → 对话管理提供“收藏”模块查看并取消收藏。收藏不改变普通列表排序、不读取或外发会话内容；归档和移入回收站会自动取消收藏。
- **Desktop：** 待实现。Desktop 尚无同等会话收藏持久化与管理列表 owner，不显示伪菜单项或设置模块。
- **建议：** 只复用现有会话长按菜单、左侧列表右滑操作带和设置 → 对话管理，不在聊天主页、Composer 或会话详情新增常驻按键。

### 2026-08-26 全局字体大小（新增）

- **Android：** 设置 → 外观提供“小／标准／大”字体大小；标准保持既有字级，小／大立即持久化并通过应用根文字样式全局生效，不改变图标、卡片、按钮或触控热区。
- **Desktop：** 待实现。Desktop 尚无同一外观偏好与全局文字样式 owner，不展示伪设置。
- **建议：** 仅保留在“设置 → 外观”，不在聊天、Composer、会话详情或工作页增加常驻按键，避免误触改变阅读密度。

### 历史规则（2026-08-20；仅保留审计背景）

- 每一项面向普通用户的新功能，必须在同一改动中登记到 Android 与 Desktop 的“设置 → 功能审阅”；缺少任一端不得标记交付。
- 每条登记必须写明：功能名称、当前去留状态（默认“待您判断保留或删减”）、现有设置入口、是否建议在常用界面新增按键，以及一条小字建议和理由。
- 默认仅提供设置二级入口；若建议在聊天、Composer、会话详情或工作页新增按键，必须在“功能审阅”中明确写出建议位置和误触/隐私影响，待用户判断后才可加。
- 凭据、Provider 诊断、fixture、离线 Eval、工程控制不得借“功能审阅”进入普通设置；未具备真实 owner 的候选能力仅可作为“待您判断”的审阅条目，不得展示为可用配置或执行入口。
- 当前登记项：ChatGPT / Claude ZIP 导入（建议保留设置入口、不加对话主页快捷键）；未关联媒体人工关联（建议仅在存在候选时于批次详情提供二级操作）；Desktop Compare 联网执行（待您判断，阶段 1–5 已完成 fail-closed owner、Security.framework 边界和未注册 mock-only adapter/安全 receipt；建议复用既有“对比”操作，不新增 Composer 常驻按钮；固定预设与状态仅由设置 → AI 模型服务承载）；本地精确复用（待您判断，仅双端离线精确键/既有消息引用索引，未接入普通聊天或 Provider；建议暂不增加常驻按键，未来真实复用、用量和清理完整后只在设置提供控制）；跨端文本会话交换（待您判断，Android 仅导出当前活动、未归属项目且无草稿/附件/工具结果的文本会话，Desktop 复用既有工作区导入；建议仅保留设置二级入口，不加聊天或 Composer 按键，且不把它写成备份、云同步或完整工作区保真）；完整工作区交换（v2）（待您判断，Android 从设置 → 数据与导入选择单个 v2 包，仅空本机可恢复，已有本机数据或待恢复记录均脱敏拒绝；Desktop 从设置选择单一 v2 包私有导入；建议只保留双端设置二级入口，不加聊天、Composer 或工作页按键，且不把它写成备份、云同步或对已有本机数据的覆盖）；Provider 连接自检（Android 仅在设置 → AI 模型服务中、用户主动点按后发送固定 `hi` 和 1-token 上限，不发送对话、附件或上下文；失败仅进入 7 天本机脱敏诊断，不增加聊天/Composer 按键；Desktop 尚未具备同一真实发送 owner，故只登记为待实现而不展示伪入口）；附件断点续传中转（待部署后再由您判断是否开放：Android 仅在“用户点击发送”后，用用户自托管的 HTTPS 临时网关上传准确已提交附件，并持续显示“南枫受控上传中转 → 当前服务商”；不增加 Composer 常驻按键；当前未配置网关时仍走现有直连完整附件链。Desktop 尚无同等发送 owner，只登记为待实现，不展示伪入口）。schema 38→39 覆盖升级计数审计仅为隔离验收包内无 UI、无内容的工程诊断；不属于 Android 或 Desktop 面向用户的功能审阅项，也不新增入口或按键。

### 2026-08-25 批量清理已归档与回收站（新增）

- **Android：** 设置 → 对话管理中的“已归档”和“回收站”各自提供低频批量清理入口。清空已归档只会把已归档对话批量移入回收站，仍可恢复；清空回收站才会永久删除其中的本地会话消息树和从属记录，操作前显示影响数量并要求单独确认。已保存的记忆摘要和费用统计不随会话永久删除而删除。
- **Desktop：** 待实现。Desktop 尚无同一会话生命周期与永久删除 owner，不展示伪清理操作。
- **建议：** 仅保留在两个会话管理页，不在聊天主页、Composer 或会话详情增加常驻按键；归档清理保持可恢复，回收站永久删除必须有显式确认。

### 2026-08-25 截图证据优先分析（新增，已按用户确认保留）

- **Android：** 已启用真实 owner。用户在提交图片、PDF、视频、音频或文件时明确要求“分析、解读、核实、判断、评估”等，普通聊天不论当前是标准、深度或具体哪一个已接入模型，都会附加同一份证据优先合同：先给结论，区分已核验事实、材料观点、推理、反例/条件、风险和下一步；不以截图逐段转述充当分析。该类明确分析请求自动使用所选服务商已核验的联网路径：OpenRouter 任意模型使用 `openrouter:web_search`，千问任意档位使用其 Chat Completions 联网检索；已有深度联网路由保持不降级。DeepSeek 当前没有可与材料同发的联网序列化 owner，不能伪装已联网，会如实说明未取得实时来源。
- **Desktop：** 待实现。Desktop 尚无同一普通多模态发送和 Provider 检索 owner，不展示“已按截图联网分析”的伪状态。
- **建议：** 不新增设置开关或 Composer 常驻按钮；用户已在提交材料并明确要求分析时给出意图。普通看图、转写或未要求分析的附件消息不自动联网，避免额外成本与不必要外发。

### 2026-08-25 通知与提醒设置（新增）

- **Android：** 已启用真实 owner。设置首页新增“通知与提醒”二级入口，三个开关均立即保存在本机：关闭“计划监控结果通知”后，后台监控仍按用户创建的计划运行并把结果保存到任务列表，但不再发送系统通知；关闭“对话提醒建议”后，不再在高价值完成对话末尾显示“添加提醒 / 监控”；关闭“对话未读提醒”后，不再显示左侧列表的橙色未读标记。未读项是本机列表标记，不伪装成系统推送；计划监控系统通知仍须另行取得 Android 通知权限。
- **Desktop：** 待实现。Desktop 没有同等持久化通知设置、后台监控与已读水位 owner；功能审阅只如实登记待实现，不显示无效开关或伪通知。
- **建议：** 保留设置二级入口和三项独立控制，不在聊天主页、Composer、会话详情或计划列表增加重复开关；用户可按提醒干扰程度分别关闭，且不会影响既有本地结果、任务或对话事实。

### 2026-08-25 计划监控与对话提醒（新增，已按用户确认保留）

- **Android：** 已启用真实 owner。左侧栏搜索下方的“已计划”打开全屏计划页，可手动创建、查看最近结果、暂停或删除任务；“添加提醒 / 监控”不再跟随每条完成回答显示，只在当前一问一答本地命中高价值、可长期跟踪的少量主题时出现。它会在设备本地把当前用户问题和完成的助手答复整理为短名称、明确监控要求和频率的可编辑草案；用户保存前可修改或取消，不会自动创建任务，也不会发送对话正文或附件。首次保存后以网络受限的唯一 WorkManager 任务执行；每次仅发送用户确认的任务名称和监控要求，使用 5.6 Terra 标准档与已配置的实际服务商官方网页检索，结果、token 与安全错误码写入本机 Room。系统结果通知须经 Android 通知权限单独授权。
- **Desktop：** 待实现。Desktop 没有同等的持久化任务、受控后台执行、模型调用记录和通知权限 owner；功能审阅如实登记为待实现，不显示“已计划”或假运行结果。
- **建议：** 保留左栏“已计划”以及仅在高价值对话末尾出现的入口；普通开发、闲聊、一次性解释与无明确长期跟踪价值的会话不显示入口，避免把聊天页变成任务流。计划名称应为 20 字内的内容概括；表单必须继续明确外发范围和模型成本，暂停或删除后不得继续联网。

### 2026-08-25 普通聊天后台继续生成（既有发送可靠性修复）

- **Android：** 已启用真实 owner。既有发送、重试和停止入口不变，不新增 Composer 或设置常驻按键；用户触发后的网络请求和流式落库由前台服务实际持有，通知只表示当前生成状态。回到应用后从本机 runtime/attempt 回读，失败仅展示安全错误与既有重试入口。
- **Desktop：** 待实现。Desktop 没有同等的前台执行/系统通知 owner，不显示“可后台继续生成”的伪状态。

### 2026-08-24 对话未读提醒（新增，现由通知与提醒设置控制）

- **Android：** 已启用。会话在当前未打开期间收到新 Assistant 内容时，仅在左侧列表日期右侧显示主题色小圆点；进入会话立即清除。设置 → 通知与提醒可关闭该显示，不影响本机已读时间水位，也不新增聊天主页、Composer 或会话详情按键。
- **Desktop：** 待实现。Desktop 尚无同等会话已读 owner，因此不得展示伪提醒；具备同等本地 owner 后，才在设置 → 功能审阅以同样的入口建议登记。
- **建议：** 保留为列表内的被动状态提示，不增加任何常驻操作，避免把阅读状态误解为发送、联网或内容分析。

### 2026-08-25 个性化与记忆摘要（新增）

- **Android：** 设置 → 个性化是唯一入口，包含启用记忆、基础风格和语气、记忆摘要、昵称、职业与自定义指令，以及“高级”下的资料库搜索。非空且已保存的自定义指令用于每次普通对话，不受记忆开关影响；启用记忆时，用户资料与相关 ACTIVE Memory 也会先在本机检索后加入本次上下文；资料库搜索默认开启，相关 ACTIVE 本地资料同样会先在本机检索后加入。两类检索均只发送相关的、预算内的完整片段，绝不将全库无差别外发；关闭对应开关、无命中或超出预算则不加入。基础风格行下方仅说明它影响对话语气而不改变功能。记忆摘要进入独立页面：顶部居中标题与更新时间，右侧菜单可查看说明、回读本机摘要，或经确认软删除摘要并关闭记忆；底部“询问或更新”明确分为仅本机查询与用户确认后的本机补充，绝不把输入静默发送给模型。用户在聊天中明确发送“记住了”时，系统只将模型按严格格式整理出的非敏感长期信息写入本机摘要；少量符合稳定个人/项目/偏好信号的已完成对话末尾可显示“加入记忆摘要”，仍须用户点击确认。关闭启用记忆后，不检索或发送摘要内容。资料、摘要正文、提示词、回复与 Key 都不写入调用记录或诊断。
- **Android：** 个性化页“自定义指令”默认显示五行编辑区；标题右侧的全屏图标打开独立的长文本编辑面。全屏页标题居中，右侧主题色圆形勾号保存当前文本并返回；个性化页既有保存勾号也统一为圆形框。默认与全屏计数区均压缩为 `5dp` 底距，避免数字上下形成无效留白。关闭全屏仍只返回原页不隐式保存；不新增常驻入口、不会发起模型调用或改变隐私边界。
- **小字理由：** 短编辑区让设置页先呈现完整决策信息；全屏仅在用户处理长指令时提供足够阅读和编辑空间。
- **Android：** 搜索页中，附件普通点击直接打开本地完整预览，不增加常驻入口；长按文件显示“快速定位到对应对话”和“删除附件”。删除须二次确认，只解除当前消息引用，不删除消息或对话；共享文件只在最后一个本机引用消失后清理。所有搜索文件卡／行同时显示持久元数据中的真实文件大小。“全文／正文”结果仍直接打开原有本地会话；进入会话后，用户向左或向右横滑均可恢复原搜索页及其当前查询、分类和结果。Desktop 尚无同等搜索附件 owner，不展示伪入口。
- **小字理由：** 普通点击优先查看；定位和破坏性删除都收进低频长按面，既避免误触，又让用户能从统一搜索入口安全管理单个附件而不误删整段历史。
- **Desktop：** 待实现。Desktop 尚无同一普通发送、个性化设置与本地 Memory 检索的完整 owner，因此只能在 Desktop 的功能审阅中登记为待实现，不能显示可工作的开关或声称已随模型调用。
- **建议：** 保留设置二级入口与低频对话尾部确认，不在聊天主页、Composer 或会话详情增加常驻按键；避免误触、自动记忆或外发歧义。

### 2026-08-24 关于与版本信息（新增）

- **Android：** 已启用。设置 → 关于只读显示南枫 AI 的运行时版本号与构建号，并提示数据与权限管理仍在“隐私与安全”；不添加无 owner 的帮助中心、条款或许可证按钮。
- **Desktop：** 待实现。Desktop 尚未接入同一可验证的应用版本 owner，因此不显示静态或伪造版本页。
- **建议：** 保留 Android 设置二级入口；不在聊天主页、Composer 或会话详情添加按键，版本信息属于低频只读信息。

### 2026-08-24 素材类型标签（新增）

- **Android：** 已启用。普通多模态模型调用按提交顺序传入 `<图片>`、`<PDF>`、`<视频>` 等真实类型标签；回答涉及材料时先单独标注对应标签，正文使用具体类型，不再笼统称为“附件”。标签只存在于本次模型请求，不写入对话正文、调用记录或诊断。
- **Desktop：** 待实现。Desktop 尚无同一多模态普通发送 owner，不得显示伪标签或声称已传入素材。
- **建议：** 保留为消息生成时的透明说明，不新增聊天主页、Composer 或会话详情按键，避免将素材标签误解为新的发送或管理操作。

### 2026-08-24 后台继续生成（新增）

- **Android：** 已启用。用户开始普通模型生成时，App 启动可见的系统前台持续任务；按 Home 或短时切后台后，已有流式请求继续运行，完成或用户点停止后即移除通知。网络断开、系统强制终止或用户强制停止 App 时，无法安全断言服务端是否完成，仍保留既有“结果未知 / 按原编号重试”路径。
- **Desktop：** 不适用。Desktop 没有 Android 前台服务 owner，不展示伪通知。
- **建议：** 不新增聊天主页、Composer 或会话详情按键；只在实际生成期间显示系统状态，避免把后台运行误解为自动重试或后台新增请求。

### 2026-08-23 逻辑模型路由（保留）

- Android Composer 保留既有模型入口，不新增主页按键：自动、对比、日常、深度、图像/视频/PDF 五个逻辑槽位；日常、深度和媒体通过同一白色二级选择面展开。普通发送无逐次确认、每次只向选定模型发起一次请求；失败不静默换模型或跨服务商重发。
- Android 设置 → AI 模型服务的配置页只保留自动路由、OpenAI/Claude/Gemini（共享 OpenRouter）、Qwen 官方直连、DeepSeek 官方直连、智谱官方直连、模型选择、加密 Key 和用户主动连接测试；DeepSeek 的同一入口承载 V4 Pro 与 V4 Flash，不增加服务商按钮。调用记录、上下文选材和连接诊断都改为独立入口。调用记录显示状态、实际服务商/模型、Token、耗时和安全错误；上下文选材显示本次实际选用的本地资料标题、类型与估算 Token；三者均不显示 Key、对话正文、附件、提示词或完整响应。
- 2026-08-30 增量审阅：`GLM-5.3` 复用既有“智谱”设置分段、同一加密 API Key、既有 Composer 模型选择和调用记录，不新增主页、Composer、侧栏或设置常驻按键。它进入“深度”及 Auto 三条路线；MD／PDF／图片／视频沿用统一附件解析层，不另造文件入口。Android 保留；Desktop 只有目录登记，在正式直连发送 owner 完成前不得宣称可调用。
- Desktop 设置同步登记同一逻辑模型目录、调用记录和上下文选材字段；Desktop 尚未具备正式多服务商发送 owner，因此不得把静态目录或设置文案写成已可真实调用。

### 2026-08-23 本地上下文索引与选材诊断（保留）

- Android 普通发送仅在本机 FTS5 索引中召回有限的 ACTIVE Memory、Knowledge 与历史摘要；中文使用二字检索项，按会话/项目范围过滤并跨资料去重。当前问题、完整附件本地输入估算和附件提示先预留 Token，剩余容量才分配给近期历史与检索资料；超限会本机停止，绝不删掉当前问题或把 PDF/视频替换为首页/封面。模型设置的本机诊断区只显示最近选中的资料类型、标题、稳定 ID、估算 Token 与索引可用状态，绝不显示资料正文、提示词、附件、回复或 Key。
- Desktop 同步登记为“设置 → 功能审阅”的后续实现项：只有在拥有同等本地索引与真实普通发送 owner 后，才能显示同样去内容化的选材诊断；当前不得把 Android 的索引实现写作 Desktop 已可调用。
- 更多本地控制面（待您判断，Android 设置二级页进入既有 CONTROL，Desktop 设置二级页分别进入已有 Projects、知识与关系、长期 Memory；建议仅保留设置二级入口，不在聊天主页、Composer 或会话详情新增按键，避免将本地管理误解为发送、联网或自动执行）。

### 2026-08-23 对话尾部与生成状态（保留）

- Android 复用既有对话消息区：助手消息的复制、分享、分支和时间作为同一尾部组贴消息右侧；持久 `PARTIAL` 助手消息显示短暂的“正在生成/正在继续生成”进度提示，完成、失败和取消后自动消失。它不是新的执行入口，也不新增聊天主页、Composer 或会话详情常驻按键。
- Desktop 同步登记为待实现的呈现一致性项：在其拥有同一持久流式消息状态 owner 前，不展示伪造的生成进度，也不将 Android 的状态写成 Desktop 已可用。
- 小字建议：保留为消息内临时反馈；它只反映本地已持久化的 `PARTIAL` 状态，避免把尚未接收服务端结果的请求误写为“已完成”。

### 2026-08-24 助手结果模型归属（保留）

- **Android 保留：** 每条助手结果尾部在既有操作/时间组下只显示模型的具体名称；普通、自动、对比和按原 Attempt 重试均从该助手消息绑定的实际 `NormalChatSendAttempt` 读取，不从当前 Composer 选项、Auto 策略或目录刷新结果猜测。历史/导入/未绑定记录不伪造模型或费用事实。
- **官方实时检索：** 深度／日常菜单仍是既有模型选择入口，不新增按钮；OpenRouter 显示“OpenRouter · 官方实时联网检索”，千问显示“千问 · 官方实时联网检索”。DeepSeek V4 Pro 与 V4 Flash 只通过 DeepSeek 自身官方 Responses 路径执行已核验的网页检索，绝不把 DeepSeek 模型 ID 转发给千问或 OpenRouter；关闭联网或请求不满足当前信息信号时仍走既有 Chat Completions 文本路径。
- **入口与按键：** 不新增聊天主页、Composer、会话详情或设置常驻按键；这是既有消息尾部的只读透明度信息，和复制、分享、分支、时间保持右对齐。
- **Desktop：** 登记为“设置 → 功能审阅”的待实现一致性项。Desktop 没有同一普通发送 Attempt 与助手消息归属 owner，当前不得显示 Android 的模型名或伪称已完成。
- **小字理由：** 将显示事实固化在发送时，可避免 Auto、切换模型、重试或后续模型目录更新将历史回答错标为当前选择；归属记录不保存提示词、附件、回复正文或 Key。

### 2026-08-25 对话费用与用量（新增）

- **Android：** 已启用真实 owner。设置 → AI 模型服务新增“费用与用量”入口，显示每条已完成助手对话的输入/输出 Token 和美元金额；消息尾部在具体模型名后同步显示同一金额。OpenRouter 回包的 `usage.cost` 是“实际金额”；未回传金额时，当前目录内全部模型（OpenRouter、千问、DeepSeek）均按版本化本机价目表显示“≈ …（估算）”，历史 token-only 记录读取时也即时补算。估算不伪称实扣，不包含响应未披露的联网工具、优惠或缓存写入费用；未知金额不会被写成零。金额、来源与 Token 只保存在消息归属记录，不保存提示词、回复、附件或 Key。
- **Desktop：** 待实现。Desktop 没有同一普通发送、消息归属与本机费用账本 owner，不显示静态金额、估算或伪入口。
- **建议：** 保留设置二级入口与只读消息尾部金额，不新增聊天主页、Composer 或会话详情按键；费用需要可追溯但不应干扰主对话流程。

### 2026-08-26 对话提醒草案整理（新增）

- **Android：** 对话尾部不再从泛化主题模板生成提醒。仅当用户在开头发言中明确要求持续跟踪／提醒时显示“添加提醒 / 监控”；点击后才将该开头用户发言与紧随的首个完成回答发送给 Qwen3.7-Plus 整理为可编辑草案。普通旅行比较、一次性问答、开发讨论以及泛泛的“价格／动态”不显示入口。Qwen 未配置、网络或结构化解析失败时不创建替代模板任务。
- **费用与隐私：** “设置 → 模型与联网 → 费用与用量”在“提醒草案整理”分组单独列出该 Qwen 调用的时间、Token、估算金额与安全状态；只保存 opaque 会话 ID 与运行元数据，不保存发送的两段文本、完整对话或附件。

### 2026-08-24 创建分支成功反馈（保留）

- **Android 保留：** 助手消息尾部的“从此处创建分支”使用分叉路径图标；成功创建并切换到新的本地对话后，在 Composer 上方短暂显示“已创建分支 / 已打开新的本地对话”。它是一次性成功状态，不新增聊天主页、Composer、会话详情或设置的常驻按键。
- **Desktop：** 仅登记为待实现一致性项。Desktop 在拥有同一“从助手消息复制前缀并切换会话”的持久化 owner 前，不展示假成功提示。
- **小字理由：** 分支创建会立即切换会话，短暂、可访问的屏幕反馈能确认点击已生效，而不长期遮挡消息或把“移动文件”误作分支语义。

### 2026-08-24 编辑消息/创建分支近满宽弹窗（保留）

- **Android 保留：** 长按用户消息后进入“编辑消息”时，编辑/创建分支弹窗使用全窗口 Dialog 中的白色内容面，左右各保留 12dp 安全边距，接近铺满手机宽度；多行编辑框从 3 行开始，最多 580dp 高，保留原有取消与创建分支动作。
- **入口与按键：** 仅调整既有“编辑消息”入口后的弹窗几何，不新增主页、Composer、会话详情或设置常驻按键；创建分支继续委派既有本机 owner。
- **Desktop：** 当前 Desktop 没有同一 Android Compose 编辑弹窗 owner，登记为待实现一致性项，不能伪称已完成。
- **小字理由：** 编辑长消息时更宽的真实阅读区域能减少不必要换行和滚动，同时保留清晰的边缘、圆角和系统手势安全空间。

### 2026-08-24 对话复制成功触感（保留）

- **Android 保留：** 消息弹窗、助手结果快捷操作，以及代码/表格的复制在剪贴板写入正常完成后提供一次系统轻触确认；Android 11+ 使用系统确认触感，旧系统回退到轻量键盘触感。系统触感关闭或设备不支持时由系统尊重用户设置。
- **入口与按键：** 不新增按键、提示或权限；复制的文本、剪贴板内容、消息、附件、Provider、Key 与网络边界均不改变。
- **Desktop：** 当前仅 Android 系统触感有同一 owner；Desktop 后续依自身平台偏好单独实现，不能伪称已完成。
- **小字理由：** 复制没有可见状态变化时，短促确认触感能让用户立即知道动作已完成，又不会把普通滚动或未成功写入误报为成功。

### 2026-08-24 Composer 自然展开（保留）

- **Android 保留：** 既有底部悬浮 Composer 由单行开始，随输入的真实换行自然增长，最多显示十行；超过十行后仅输入文字区域内部滚动。加号、模型选择和发送/停止按钮保持底部锚定，不新增按键、不改变发送语义。
- **Desktop：** 不适用 Android 悬浮 Composer 几何；Desktop 后续如拥有同一输入区，应遵循其窗口和键盘交互另行实现。

### 2026-08-24 Composer 回车换行与草稿精确保留（保留）

- **Android 保留：** 底部 Composer 的系统键盘回车始终插入换行，最多按既有十行自然展开；发送仍只由既有发送按钮执行。普通会话草稿按编辑器原值持久化，因而末尾刚插入的换行、行间空行和前后空白不会在异步保存回写时消失。
- **其它输入框：** 临时会话草稿与手工文本、对话编辑、Knowledge、Memory、项目指令及导入正文等多行文本框同样保持系统默认换行；搜索、标题、重命名、标签、URL 和删除确认等单行字段不改变，继续使用其完成/搜索语义。
- **入口与按键：** 不新增按键或入口，不改变附件、模型、Provider、Key、发送或联网边界。
- **Desktop：** 当前仅 Android Compose Composer 有同一草稿持久化 owner；Desktop 待其自身输入框实现后再单独验收，不能写作已完成。
- **小字理由：** 回车是多行内容编辑的基本输入，不应被异步保存或发送动作吞掉；单行表单保持原有键盘动作可避免搜索和命名场景误插空行。

### 2026-08-24 全部 Android 弹窗的空白点击与侧边向内滑关闭（保留）

- **Android 保留：** 所有标准确认/选择弹窗、自定义全屏 Dialog 与 Composer 模型/附件覆盖层都通过同一关闭桥处理：点击遮罩空白区域沿用原有关闭回调；从左或右近边缘向画面内横向滑动至少 72dp、且明显横向（横向至少为纵向 1.3 倍）也关闭当前弹窗。弹窗内部的纵向列表、文字编辑、图片缩放和视频控制不被手势桥消费。
- **运行中边界：** 关闭只收起界面，不伪装为取消导入、导出、离线 Eval 或读取操作；这些既有本地任务仍保持真实 owner 和可重新进入的状态。Memory 冲突关闭不写入候选，也不改变现有记录。
- **入口与按键：** 不新增入口或常驻按键，不触及消息、附件、模型、Provider、Key、外发或系统返回。
- **Desktop：** 当前仅 Android Compose Dialog 有此共享窗口 owner；Desktop 待其自身 modal/窗口结构接入后单独验收，不能写作已完成。
- **小字理由：** 用户能以统一、低记忆成本的空白点击或近边缘向内滑动退出暂时界面，同时保留内容区内原有的阅读、编辑与媒体操作手势。
- **小字理由：** 让长问题完整可读，避免固定高度把内容压在模型和发送控制下，同时为消息区保留现有浮层边界。

### 2026-08-24 Composer 临时文字工具条收口（保留）

- **Android 保留：** 点击消息区未被内容或控件消费的空白处、切换会话/表面，或 App 进入后台时，Composer 会释放文字焦点。系统“粘贴 / 自动填充 / AI 写作”工具条因而立即收起，不会作为应用状态在返回 App 后残留；不新增按键或弹窗。
- **Android 键盘：** Activity 在每次获得窗口焦点时以 `stateAlwaysHidden` 启动或恢复，默认不展开软键盘；用户点按 Composer 后仍使用系统原生键盘正常输入。
- **颜色边界：** 南枫 AI 自有的附件/模型二级菜单保持白色卡片。截图中的“粘贴 / 自动填充 / AI 写作”由 ColorOS/输入法提供，App 不能可靠重绘为白色；强行接管会破坏系统粘贴、选词与 AI 写作能力，因此保留系统配色。
- **Desktop：** 不适用 Android/ColorOS 原生文字工具条；Desktop 后续如新增同类临时菜单，应采用白色表面并在失焦时销毁。
- **小字理由：** 临时操作应跟随当前输入焦点，而非存入会话或恢复状态，避免遮挡内容和产生“退出后仍在”的错觉。

### 2026-08-24 视频预览点击即播放（保留）

- **Android 保留：** 用户点击会话或草稿中的本地视频卡后，视频预览在经过本地受控副本准备完成时立即播放；若已有本地播放位置，先定位到该位置再继续播放。底部进度、中央暂停、双击切换和边缘滑动关闭仍使用原有唯一手势 owner，不新增聊天主页、Composer、会话详情或设置按键。
- **Desktop：** 不适用；Desktop 没有对应 Android `VideoView` 预览 owner。
- **小字理由：** 点击视频卡本身已表达播放意图，自动开始能避免打开后必须再点一次播放；只使用已验证的本地副本，不触发上传、外发或后台播放。

### 2026-08-24 附件多选（保留）

- **Android 保留：** Composer 的“添加图片和视频”使用系统原生多选相册，图片和 MP4 视频在同一选择面中最多可选择 4 项；“添加文件”同样支持多选已允许的 MP4、音频、PDF 与安全文本文件。普通聊天和临时会话都复用对应的私有复制 owner；每项分别校验并入草稿，重复项跳过、超出数量或不支持项明确汇总，不影响同批有效项。
- **系统视觉边界：** 选择格右下角的勾选框、已选数和按钮样式由 Android/ColorOS DocumentsUI 或 Photo Picker 决定。App 通过多选契约让系统展示选择框，不能安全改画其对比度或形状；App 内仍清楚显示“添加图片和视频”和批次处理结果。
- **Desktop：** 当前 Desktop 没有对应 Android 私有附件发送 owner；不将此 Android 入口写作跨端完成。
- **小字理由：** 多个附件通常属于同一问题或材料组，选择一次即可逐项安全复制；选择本身不上传、不外发，只有用户点击发送时才沿用现有外发规则。

### 2026-08-24 相机原图附件保真（保留）

- **Android 保留：** Composer 的“相机”改为系统 `TakePicture` 写入 App 受控缓存 URI；普通与临时会话均从该完整原图读取，再复用已有 MIME、像素上限、私有复制、hash 和草稿 owner。相机打开或拍摄本身不上传、不外发。
- **失败与清理：** 取消、无法创建临时 URI 或原图不可读时，草稿不变并显示中文原因；原图已复制、被拒绝或取消后均删除 App 缓存中的临时拍摄源。不会再使用缩略 Bitmap，也不会把完整原图重新 JPEG 压缩。
- **Desktop：** 不适用；Desktop 没有 Android Camera/`TakePicture` owner。
- **小字理由：** 用户点相机期望获得设备拍摄的实际清晰度；缩略图路径会不可逆降低画质，而受控临时 URI 仍保持原有私有存储和不外发边界。

### 2026-08-24 用户消息胶囊气泡（保留）

- **Android 保留：** 普通、工作区和临时会话复用同一右对齐用户文本气泡；其已测量内容表面采用 50% 圆角的胶囊形状。短消息保持紧凑，长消息仍受原有 82% 宽度上限约束，文字选择、长按操作和附件组均不改变。
- **入口与按键：** 不新增聊天主页、Composer、会话详情或设置按键；这只是用户已发送文本的视觉语义，不触及消息、附件、Provider、Key 或外发流程。
- **Desktop：** 当前无同一 Android Compose 气泡 owner，登记为待实现一致性项，不能写作已具备。
- **小字理由：** 胶囊轮廓可更清楚地把用户表达与开放式助手内容区分开，同时不牺牲长文本可读宽度或既有操作入口。

### 2026-08-24 对话初始内容避开顶部悬浮栏（保留）

- **Android 保留：** 普通、工作和临时会话的消息列表共用 64dp 顶部内容 inset。短对话或刚开始的内容从菜单、对话/工作切换和临时聊天按钮下方开始；该 inset 属于 `LazyColumn` 的可滚动内容内边距，不绘制白色顶栏或新增固定布局。
- **滚动边界：** 内容超过视口后，顶部 inset 会与消息一起向上滚动；用户继续上滑时，消息可自然进入顶部悬浮控件下方。既有底部 Composer 的 86dp 内容安全距离、滚动、跳到最新和右侧位置条不变。
- **Desktop：** 当前仅 Android Compose 有对应消息列表 owner；Desktop 登记为待实现一致性项，不能写作已具备。
- **小字理由：** 短对话先保持导航与正文的清晰分离；长对话仍保留连续阅读面积，不用固定白条浪费可视区域。

### 2026-08-24 有内容对话的右上新对话与完整菜单（保留）

- **Android 保留：** 当前普通或工作会话已有消息时，顶部中间的“对话 / 工作”切换隐藏，右上显示同一白色胶囊内的“新对话”和“更多”图标；空会话仍保留既有中间切换和临时聊天入口。更多菜单显示当前会话标题，并提供分享、置顶/取消置顶、添加/移动项目、已上传文件、在聊天中查找、添加到主屏幕、归档/恢复和删除。
- **真实功能边界：** 分享仅向 Android 系统分享面交出当前本地会话的文字；已上传文件只列出当前消息中实际已发送的本地附件并沿用现有本地预览；聊天内查找只检索当前已呈现消息；主屏快捷方式仅保存不透明会话 ID，Android 桌面确认后打开对应仍有效的本地活动会话。归档、删除、置顶和项目归属继续使用既有管理 owner，删除仍先进入回收站。
- **入口与按键：** 这是用户明确要求的内容页顶部常驻操作；不在空会话增加菜单、不在 Composer 增加新按键，不读取 Key、不上传附件、不发模型请求。
- **Desktop：** Desktop 暂无同一 Android 顶部会话操作和 Launcher shortcut owner，登记为待实现一致性项，不能写作已具备。
- **小字理由：** 已有内容时将高频会话管理收束在右上可预期位置，避免与正文阅读争夺中央空间；空会话保留对话/工作切换，先帮助用户选择内容空间。

### 2026-08-24 抽屉与对话正文文字放大（保留）

- **Android：** 新建普通会话在首轮完整问答后自动整理标题，不新增常驻按键或设置开关。仅将开头用户发言与首条完整南枫AI回答发送给已配置的 Qwen3.7-Plus，生成 6–32 字会话概述；不发送附件、后续消息、个性化资料或历史记录。标题整理的 Token、估算金额、模型与安全状态在既有“设置 → 模型与联网 → 费用与用量”中独立分组；失败保持“新对话”，手动标题优先。Desktop 当前无同等 owner，不展示伪开关或伪记录。

- **Android 保留：** 左侧抽屉的普通、项目和临时会话文字统一使用 1.1 倍 Typography；已发送消息的标题、段落、引用、列表、代码块、表格和安全摘要也统一放大 1.1 倍。显式行高同步按比例调整，避免中文长段落拥挤。
- **入口与按键：** 不新增任何按键或入口；Composer 输入、图标、触控目标、附件预览和消息操作按钮不随本次正文阅读调整而变大。
- **Desktop：** 当前仅有 Android Compose 的对应排版 owner，Desktop 登记为待实现一致性项，不能写作已具备。
- **小字理由：** 会话列表和连续对话是高频阅读区，适度放大可提高识别性，同时保留原有布局密度与触控几何。

### 2026-08-24 文件预览顶部操作栏按单击切换（保留）

- **Android 保留：** 图片、PDF、视频、音频和文本预览统一复用临时顶部操作栏状态。单击内容区域显示顶部关闭、下载、分享；再次单击隐藏。视频的中央播放/暂停、时间轴和顶部操作栏共用这一状态，重新显示后统一在 12 秒后自动收起。
- **操作边界：** 顶部按钮在可见时仍优先执行关闭、下载或分享，不会被单击显示/隐藏手势替代；PDF 翻页、视频双击播放/暂停、边缘滑动关闭、图片缩放、文本选择与音频播放不改变。
- **Desktop：** 当前没有同一 Android 本地文件预览 owner，登记为待实现一致性项，不能写作已具备。
- **小字理由：** 预览时可减少顶部操作对图片、视频和文档的遮挡；需要操作时只需一次明确单击即可找回，避免每种文件各自记忆不同手势。

### 2026-08-24 Composer 具体模型名完整显示（保留）

- **Android 保留：** Composer 的模型选择区固定为 216dp，模型文字为 13sp，显示当前实际路由的具体模型名，如“Claude Opus 5”或“GPT-5.6 Terra”；不再显示服务商、`Auto` 或“发送时按能力选择”等路由说明。对比选择以“GPT-5.6 Sol / Claude Opus 5”这类双模型名完整显示。
- **入口与按键：** 不新增 Composer 按键；点击模型名仍打开既有模型菜单。服务商和官方联网说明继续只在模型菜单/消息归属中展示，不把传输边界误写入输入框。
- **Desktop：** 当前仅 Android Compose Composer 完成此布局，Desktop 登记为待实现一致性项，不能写作已具备。
- **小字理由：** 输入框内最需要的是当前会由哪个模型回答；去掉路由说明并给双模型足够宽度，可避免截断和误认，同时不牺牲既有发送操作。

### 2026-08-24 Composer 分层模型选择面（保留）

- **Android 保留：** 点击既有模型名后，选择面扩至 336dp 宽的底部浮层，含半透明遮罩、圆角白色表面、拖拽把手、居中标题与关闭/返回按钮。首层按“自动选择 / 按任务选择”组织；进入分类后显示“选择具体模型”，每项以名称、用途说明、右箭头或选中勾分层呈现，长列表在面内滚动。
- **交互边界：** 自动、对比、日常、深度、图像/视频/PDF 的原有分类、模型 ID、当前会话持久化和回到上一层语义不变。深度模型继续如实标注实际官方实时检索接收方；点击遮罩、关闭或返回只关闭/回退选择面，不发送消息、不读取 Key、不调用 Provider。
- **入口与按键：** 不新增 Composer 或聊天主页按键；仅优化既有模型选择入口的视觉层次和文本布局。
- **Desktop：** 当前仅 Android Compose 有此模型选择面 owner；Desktop 登记为待实现一致性项，不能写作已具备。
- **小字理由：** 分类与具体模型分开呈现，能让用途、实际模型与选中状态同时可读，避免在紧凑菜单里混成一列难以判断。

### 2026-08-24 对话右侧位置条按真实内容高度定位（保留）

- **Android 保留：** 对话右侧位置条不再按“第几个气泡”推算位置。每个进入可视区的消息行都会使用 Compose 的实际测量高度，包含文字自动换行、日期分隔、附件卡和操作行；长气泡内部继续滑动时，位置条会随真实像素距离连续前进，只有到达整段对话末尾才贴到底部。静止约 1.2 秒后淡出，重新滚动会立即显示。
- **入口与按键：** 不新增任何聊天主页、Composer、会话详情或设置按键；位置条保持原有右侧低干扰呈现，不可点击，也不改变消息滚动、跳到最新消息、阅读进度或发送逻辑。
- **Desktop：** 当前 Desktop 没有同一 Android Compose `LazyColumn` owner，登记为待实现一致性项，不能写作已具备。
- **小字理由：** 长文本对话的可读长度取决于实际换行而不是气泡数量；按最终布局高度定位，能避免长气泡中位置条停住、跨到下一条消息才突然跳转的误导。

### 2026-08-24 对话左侧抽屉严格横向手势（保留）

- **Android 保留：** 左侧抽屉仅在画布上明确向右滑动至少 56dp、且手势角度不超过相对水平 50°时打开。纵向位移一旦大于横向的 1.19 倍，就作为上下滚动交给正文、模型菜单和其他子控件；不会再因为七八十度的斜向上/下滑误开抽屉。抽屉打开后仍保留原生的关闭拖动和遮罩交互。
- **入口与按键：** 左上角菜单按钮保持原有直接打开行为；不新增聊天主页、Composer、会话详情或设置按键，也不改变系统边缘返回手势。
- **Desktop：** 当前 Desktop 没有同一 Android Compose 抽屉手势 owner，登记为待实现一致性项，不能写作已具备。
- **小字理由：** 模型菜单和长正文以垂直浏览为主；严格方向判定优先保证连续阅读，同时保留明确水平拖动这一已有导航路径。

### 2026-08-24 设置分层返回栈（保留）

- **Android 保留：** 设置导航按实际进入顺序保存路径：设置主页 → 设置分类 → 导入中心/更多本地控制面 → 其子工作区或导入任务。顶部返回、系统返回和设置页内的返回入口都只弹出当前一层；例如导入任务先回导入中心、导入中心再回“数据与导入”、再回设置主页，只有设置主页返回才回对话抽屉。
- **入口与按键：** 不新增任何常驻按键。左上角/页面“返回上一级”沿用现有返回入口，系统返回与应用内返回结果一致；模型、备份和其他自身已有弹层仍优先由各自的关闭/返回 owner 处理。
- **Desktop：** 当前仅 Android Compose 设置 owner 完成此栈行为；Desktop 登记为待实现一致性项，不能写作已具备。
- **小字理由：** 设置中的数据、导入和控制面可以进入多层内容；逐层回退保留用户刚才的上下文，避免深层页面一次返回到对话桌面而丢失位置。

### 2026-08-24 左侧会话行右滑管理入口（保留）

- **Android 保留：** 普通对话和工作区左侧抽屉中的每条会话，向右滑动会露出四个清晰的快捷按钮：置顶/取消置顶、收藏/取消收藏、重命名、删除。收藏紧跟置顶，复用同一个本机持久化 `ConversationManagementAction`；重命名和删除仍复用原有输入框及“移入回收站”确认弹窗，删除绝不一键物理执行。
- **入口与按键：** 不在聊天主页、Composer 或设置新增常驻按钮；手指向右滑动才显示操作，列表点击仍只打开会话，长按菜单继续可用。
- **Desktop：** 当前 Desktop 没有对应 Android 抽屉行与同一手势 owner，登记为待实现一致性项，不能写作已具备。
- **小字理由：** 会话置顶、收藏、改名和回收常用但长按不易发现；显式滑动后才出现的四按钮提升可发现性，同时保留长按路径和删除确认以控制误触。

### 2026-08-24 左侧“最近”批量编辑与回收（保留）

- **Android 保留：** 普通对话抽屉的“最近”标题右侧提供铅笔图标。点击后，置顶与最近的每条会话均出现复选框，设置/新对话上方显示“全选、删除 N、完成”。编辑态会暂时收起并停用行右滑与长按，避免手势冲突；“完成”不改动任何会话。
- **删除边界：** “删除 N”只有存在选择时可点，并必须经一次“移入回收站”确认。确认后由 `ConversationFoundationViewModel.softDeleteConversations` 为每个已选会话提交既有软删除 intent，准确汇总成功或并发拒绝；消息、附件、调用关联不物理删除，仍可在设置 → 对话 → 回收站恢复。
- **Desktop：** 当前 Desktop 没有相同 Android 抽屉编辑 owner，登记为待实现一致性项，不能伪称已具备。
- **小字理由：** 多条旧会话整理适合一次选择；把复选框和结果明确的底部操作只放在显式编辑态，避免普通浏览时误触或把删除误解为永久清除。

### 2026-08-23 工作区项目树与独立会话入口（保留）

- **Android 保留：** 顶部既有的“对话 / 工作”切换仍是会话表面的唯一选择入口；左侧抽屉随当前表面投影，普通对话只显示 `CHAT`，工作区只显示 `WORK` 且按本机 `ProjectDomain` 项目分组，临时对话只显示其独立的短暂恢复说明与退出入口。三者不再复用同一会话列表或选中状态。
- **Android 保留：** 工作区抽屉标题为“项目”，右侧提供“项目列表菜单”和“创建项目”；每个项目文件夹提供项目菜单及“新建工作对话”。这些是用户明确要求的导航入口，不在普通聊天主页、Composer 或会话详情新增常驻按键。项目创建、置顶、归档和编辑均继续走既有 `ProjectViewModel → ManageProjectUseCase`。
- **边界与小字理由：** 项目仅是本机分类与指令所有者，不读取或写入外部文件夹，也不把 Codex 的资源管理器、永久工作树或任何服务商能力伪装为已实现。归档项目下原有工作会话不会被删除；临时对话不会混入任何持久会话列表。
- **Desktop：** 尚未实现同一项目抽屉；本次 Android 导航修复不能声明 Desktop 已具备对应入口。Desktop 后续需按其自身存储 owner 单独设计和验收。

## 入口矩阵

Desktop 的 v2 已提交回导仍为“设置 → 数据与导入”的二级入口：用户只可选择匿名已提交私有记录，再经 native save picker 输出。Android/Desktop 功能审阅均写明此限制；不新增聊天主页、Composer 或工作页按键，且 Android 本轮不新增导入/回导能力。

| 功能 / 实现 owner | Android 入口 | Desktop 入口 | Settings 入口 | 普通用户适合度 | 缺口 | 风险 | 最小验证 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 本地对话、草稿、附件与会话管理 / `ConversationFoundationViewModel` | 对话 Composer、Drawer、长按会话、搜索；用户点击发送才将当前草稿附件发给当前显示的接收方；未知/失败 Attempt 在 Composer 上方仅显示“按原编号重试 / 标记失败” | Chat Composer、侧栏、消息工具；本轮未增加 Desktop 附件外发或 Attempt 恢复 owner | Android 对话；Desktop 隐私与数据管理 | 是 | Android 的完整 PDF/视频请求尚待真实账号文件验收；Desktop 不得伪称已具备同一能力 | 选择、预览、草稿和删除均不得后台外发；不得发给日志/统计；Attempt 重试固定原 provider/model/idempotency key，若服务端不保证去重必须提示可能重复收费；Qwen 使用独立兼容协议 Adapter，DeepSeek 仍拒绝未声明的原始附件；不把首页/封面称为完整解析 | Android JVM Attempt/附件序列化/能力门合同、后续真实服务文件验收 |
| 自托管附件断点续传中转 / `ResumableAttachmentUploadCoordinator` + `upload-gateway` | 待自托管 HTTPS 网关部署、配置并由您决定开放后，仅复用既有点击发送；不新增 Composer 按键 | 不存在同等 Desktop 普通发送 owner；只登记待实现 | Android 设置 → 功能审阅（待判断，不展示未配置入口）；Desktop 设置 → 功能审阅（待实现） | 仅部署并启用后 | Android 尚未接入用户可配置网关与真实 Provider URL 回归；Desktop 未实现 | 默认关闭；预览/草稿/删除不上传；Room 只保留 offset/会话 ID/SHA，不存 URL、令牌、路径或字节；网关不持有 Provider Key，短时 URL 只发给当前 Provider；视频 URL 必须按模型/底层服务商能力显式开放 | Android coordinator/Room 合同；网关在受控 HTTPS 域名、持久卷和过期清理条件下的集成验收；真实 OpenRouter 图片/PDF 与按模型的视频回归 |
| Compare 显式直接执行 / `CompareVisibleExecutionOwner` → `CompareExecutionApplicationOwner` | 模型菜单行、Composer `对比`、模型长按 | 同三入口；直接显示 Desktop 不可执行状态 | 双端设置 → 功能审阅（仅去留审阅）；Desktop 设置 → AI 模型服务（仅 preset/status） | 是（仅显式外发动作） | Android 的非空草稿真实直接执行仍需用户给出的非敏感短文本、已配置凭据与 HTTP 授权；Desktop 只有未注册的 mock-only adapter，尚无 UI/Tauri/真实凭据/HTTP 组合 | 空草稿不触及 owner；附件拒绝；Desktop 不得假装外发 | Android/Node 三入口合同；Desktop 阶段 5 Rust mock adapter/内存 receipt 合同；真实外发不在本批 |
| 当前对话导出 / `ConversationFoundationViewModel` | Drawer → 设置 → 对话 → 导出当前对话 | 无同语义 owner | Android 有；Desktop 仅工作区交换包导出 | 是 | Desktop 不应把工作区交换包伪称当前对话导出 | 导出范围必须准确 | Android 既有导出读回；Desktop 仅声明当前能力 |
| 本地工作区交换包 / Android `ExportConversationExchangeUseCase` + SAF output port；Desktop Rust workspace exchange owner | 设置 → 对话 → 导出当前文本会话到 Desktop（仅当前活动、未归属项目、无草稿/附件/工具结果） | 工作模式导入入口、顶部导出 | Android/Desktop 设置 → 功能审阅；Desktop 数据设置已有导出 | 是（Android 仅窄范围文本会话） | Android 尚无项目、Knowledge、Memory、关系、附件或导入 mapper；完整跨端出口仍未关闭 | 严格预检、SAF 回读；Desktop 导入为独立工作区，不能覆盖 | Android 领域/入口合同；Desktop Rust preflight/re-export；真实 Android→Desktop 文件链待后续 |
| 完整工作区交换（v2） / Android scope→mapper/package→SAF export 与 OpenDocument→strict reader→atomic restore owner；Desktop `p6_workspace_exchange_v2` private owner | 设置 → 数据与导入 → 完整工作区交换（v2）→ 显式范围 → 系统保存位置；或选择单个 v2 包，仅空本机恢复 | 设置 → 数据与导入 → 完整工作区交换（v2）；Desktop 还可回导匿名 committed record 到 native Save picker | Android/Desktop 设置 → 功能审阅 | 候选；Android v2 导出 + 空本机受控恢复 + Desktop v2 私有导入 | Android 已在两台项目专属 AVD 完成 DocumentsUI 恢复、同包 replay、篡改/超限拒绝、非空拒绝，以及修复后新空 AVD 的恢复→完整范围导出→同字节严格回读。macOS 已完成 Android DocumentsUI 输入→Desktop native Open/Save→committed reopen/re-export strict readback；Windows/发布仍待验收 | Android 导入只读取用户选中单 URI 的有限 bytes；strict preflight 后才进入 atomic owner，非空或 unknown recovery 一律不覆盖；完整范围二进制附件保守标为高敏感；仅独立 journal-interrupt 验收包可在首个附件提升后以一次性 app-private marker 受控终止，普通 UI 不新增入口或诊断；Desktop 只容许精确 `.nfai-exchange` 或 `.nfai-exchange.zip` | Android 定向 bridge/owner/UI 合同 + 项目专属空 AVD DocumentsUI 真文件链；Desktop Rust/UI 合同与 macOS native picker 跨端真文件链均已验收 |
| ChatGPT / Claude / 南枫知识库静态会话导入 / 各自 import task owner | 设置 → 数据与导入 | 设置 → 数据 → 三个导入任务 | 双端均有 | 是 | 无 | app-private 副本、逐项确认，正文不执行 | Android/desktop 各自 importer 合同 |
| Markdown、JSON 知识、PDF 文本、网页文本快照 / Android 各自 ViewModel | 已有 picker / Dialog handler，但 `导入与适配` 未渲染普通入口 | 无 Desktop production owner | Android 本批补到设置 → 数据与导入；Desktop 标记不存在 | Android 是；Desktop 不存在 | Android 可用功能此前不可发现；Desktop 不虚构入口 | 外部文本不可信；网页入口仍以既有独立确认 owner 为准 | Android 源码入口合同、assembleDebug |
| 本地备份与恢复 / `LocalBackupRestoreViewModel` | 设置 → 数据与导入 → 备份与恢复 | 无等价 Desktop backup/restore owner | Android 有 | 是 | Desktop 不能把交换包导入写成“恢复备份” | Android 恢复具有替换语义；Desktop 导入独立工作区 | Android 既有合同；Desktop 保持不存在 |
| 更多本地控制面 / Android `P5ANavigationViewModel` + `P5ARoute.CONTROL`；Desktop 既有 work-mode route | 设置 → 更多本地控制面 → 打开更多本地控制面 → Projects、知识（含关系）、长期 Memory | 设置 → 更多本地控制面 → Projects / 知识与关系 / 长期 Memory | 双端设置 → 功能审阅 | 是；仅可达既有本地 owner | Context 与离线 Eval 仍仅 Android CONTROL 现有范围，Desktop 不虚构相应 production owner | 不读凭据、不调用 Provider、不外发；不以内部 route/DB 注入创建对象 | `P5A_LOCAL_CONTROL_SURFACE_ENTRY_CONTRACT.md`、Android/Node UI 定向合同、后续全新 acceptance GUI |
| 模型本地目录与自动路由 / P6-G owner | Composer 模型菜单；设置模型服务 | Composer 模型菜单；设置模型与路由 | 双端有 | 是（仅本地选择） | 无 | 不得把目录/fixture 当 Provider 或凭据配置 | 现有 P6-G 合同 |
| 凭据、Provider 诊断、离线 Eval、P8 控制与 fixture | 凭据、离线 Eval、P8 控制与 fixture 不作为普通入口；Provider 诊断改为“模型设置 → 连接诊断”独立页，调用记录与上下文选材亦为模型设置内独立页 | 不作为普通入口 | Android 仅显示本机安全运行元数据；Desktop 尚无真实 owner | 用户需要时可直接定位配置、调用、选材或连接问题 | Desktop 非缺口；Android 不新增聊天入口 | 记录不含 Key、正文、附件、提示词、完整响应或原始请求 | 诊断脱敏合同、调用账本、上下文选材审计合同 |

## 本批实施选择

1. Android：只补四个已有本地 import/snapshot owner 的设置入口，不改变其解析、确认、持久化或网络边界。
2. Desktop：只补既有 workspace exchange 的设置“导入工作区”入口，复用既有 `start-import` → 预检 → 确认链路；不称其为备份恢复。
3. Compare：保留现有三种 Android/Desktop UI trigger；显式 Compare 本身就是产品命令，不再额外显示产品级确认面。Desktop 真实外发仍明确未实现（阶段 5 adapter 未注册）；Android 的非空草稿真实执行仍需用户提供非敏感短文本、外部凭据与 HTTP 授权。

## 不在本批实施

- Desktop 当前对话导出、Desktop Markdown/JSON/PDF/网页导入：当前没有可复用生产 owner，需各自的协议与数据边界合同。
- Android 项目/知识/记忆/Context 的普通 chat-first IA：需要独立的导航与信息架构决策，不能借本次“入口补齐”把控制面塞进聊天或设置。
- 真实 Compare、Provider、Key、HTTP、OPPO：均不在本批授权内。

## 2026-08-30 GLM-OCR 南枫转写

- **Android：** 用户已明确要求保留。唯一普通入口为左侧栏“定时任务”正下方的“南枫转写”，页内顶部同名标题居中；设置只在智谱模型服务中展示 GLM-OCR 能力与共用 API Key 事实。主页直接列出全部 Markdown 转写，底部独立悬浮入口选择图片／PDF；条目详情同时呈现可安全打开的原文件与无截断的完整文字。长按已完成结果仍可创建新对话并把 `.md` 加入未发送草稿。GLM-OCR 不进入 Composer、Auto、普通聊天或定时任务模型列表。
- **既有入口增强：** GLM-OCR 调用复用设置中的统一调用账本，并关联显示原文件、页数和服务商请求 ID；原始图片／PDF 与生成 Markdown 复用既有全屏搜索，在“全部／图片／文件”中可见并可直接定位对应转写详情。不增加聊天主页、Composer、会话详情或设置常驻按键，不新增第二套搜索或调用记录。
- **Desktop：** 未实现同等私有附件、Room 任务、后台执行和对话草稿 owner，因此当前不展示入口或伪可用状态。
- **入口判断：** 用户已指定侧栏入口，故不再标记“待判断”；不建议新增 Composer 常驻按键，因为该能力是显式、可能计费且会外发原文件的预处理工具。
- **证据与边界：** 读取 [GLM-OCR 南枫转写当前合同](GLM_OCR_DOCUMENT_MARKDOWN_CONTRACT.md)。自动测试与构建不等于真实智谱调用或设备视觉验收；真实 Provider、Key、OPPO 仍需独立授权。

## 2026-08-30 会话“待看”

- **Android 保留：** 待看只进入既有会话长按菜单，不在聊天主页、Composer、会话详情或设置新增常驻按键。设置后对话在原分组内优先：“已置顶”与“最近”分别排序，工作对话只在所属项目内排序；不跨组改变归属。
- **信息与清除：** 列表在标题左侧显示主题色圆点，置顶行的固定顺序是“置顶图标 → 圆点 → 标题”。待看与未读语义独立但同时存在只绘制一个点；只有真正点击进入该对话时清除，长按、滑动或其他菜单操作不清除。本机持久化只保存会话 ID 和设置时间，不保存正文或外发。
- **Desktop：** 当前没有对应会话列表 owner，不展示入口，也不从 Android 行为推断已实现。
- **证据与边界：** 当前行为只读取 [Android 当前会话界面合同](ANDROID_CONVERSATION_UI_CURRENT_CONTRACT.md)。持久化、排序、圆点顺序和点击清除的定向合同已通过；真机实际阅读动作仍由用户回读确认。
