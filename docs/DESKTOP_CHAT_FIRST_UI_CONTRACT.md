# 南枫 AI Desktop 对话优先 UI 合同

日期：2026-08-13  
状态：统一 chat-first shell 已实现；2026-09-03 已按当前 Android owner 校正 C-01 空画布、C-02 内容画布与 C-03 侧栏层级，真实 Provider、同步及隔离 Tauri 内容态仍分层待验

## 当前增量：附件预览低噪声

- 图片预览不显示“缩小／适应／放大”工具栏；原图浏览、多图切换、双击／滚轮缩放和已放大后的拖拽仍属于原有视口 owner。
- 图片、PDF、视频、文本预览不显示重复的“本机私有副本”“不会外发／执行／自动播放”等说明。加载仅提供无障碍进度状态；错误仍明确显示。
- PDF 保留页码控制；视频保留“开始本地播放”、续播位置及文件名／大小／时长；文本保留文件信息与复制、保存、分享。空搜索只保留可执行的改关键词／分类／文件类型建议。

## 当前增量：全屏搜索分类胶囊与非阻塞切换

- 全屏搜索的“全部／正文／图片／视频／音频／文件”是等槽位的筛选胶囊；选中态及其按下、悬停、焦点、阴影和裁切必须保持 `999px` 胶囊几何，不得回落为圆角矩形。
- 点击分类先在保留的搜索页内原地更新选中胶囊，下一帧才请求本机索引；不得先销毁并完整重建搜索页，造成用户点击没有立即反馈。
- 本地搜索的健康索引只比较 workspace 语义哈希与索引状态；筛选读取不能反复反序列化／哈希完整工作区。数据变更、缺失或版本不一致时才按既有事务路径重建索引。

## 当前增量：工作根页全宽统一壳

- 工作根页与项目、知识、记忆同属独立工作页：外层应用必须同步切换为全宽 standalone shell，左侧仅保留工作导航，右侧占满剩余窗口。不得把已完整渲染的 `workspace-shell` 重新塞回聊天页三列栅格，导致标题竖排、内容只占一窄列或出现大面积无主空白。
- “工作”标题和空态沿用同一右侧标题／内容容器；工作根、项目、知识、记忆以同一个数据驱动 owner 判断页面形态，而非各自重复条件。

## 当前增量：一键置底圆形控件无描边

- “到最新消息”维持白底、40px 命中区、圆形轮廓与短柔和阴影，但控件本身不得有可见的 `border`，尤其不得出现深色硬边圈。表面、阴影、悬停与焦点必须属于同一圆形 owner；不改变显示阈值、平滑滚动、输入框聚焦或消息列表 owner。

## 当前增量：窗口位置与尺寸记忆

- `1440×900` 仅是首次启动或无效记录时的默认几何，不得覆盖用户上次正常关闭前的窗口位置、尺寸或最大化状态。
- 原生窗口 owner 仅在正常前台启动时，从应用配置目录的设备本地 `window-state.json` 恢复并在移动、缩放及关闭时原子更新。窗口状态不属于工作区、备份、导入或加密同步内容。
- 恢复前验证最小尺寸、合理上限和当前显示器可见性；显示器拔除、缩放变化或状态文件损坏时，保留可用尺寸或退回默认，不得把窗口启动到屏幕外，也不得因状态读写失败阻断应用。

## 当前增量：回答更多操作与模型选择遮罩

- 正文代码块和行内代码的灰底、双端平台差异、无外框、语言／复制栏及语法配色规则统一见 `ANDROID_CONVERSATION_UI_CURRENT_CONTRACT.md`；复制仍走现有原文剪贴板 owner，代码内容不参与 HTML 执行。
- “本次回答信息”采用 Android 同款纵向层级：回答设置、上下文来源分组、完整换行的来源标题、固定底部“知道了”；来源事实与旧记录兼容规则统一见 `ANDROID_RUNTIME_CONTEXT_CURRENT_CONTRACT.md` 第 5 节。不得将字段标题作为风格值，或将联网空值当作服务商返回结果。
- Assistant 回答底部三点的“更多操作”必须是锚定在真实按钮旁的小型两行菜单，而不是居中 Dialog；无标题、关闭键或整页 Scrim。菜单保留“本次回答信息”“创建分支”两个既有动作，空间不足时向上翻转并限制在视口内。
- 鼠标／触控板主键在三点按钮 `pointerdown` 时直接打开菜单，键盘激活保留无指针 `click` 回退；不得只依赖可能被 WebKit 惯性滚动吞掉的 `click`。点外、Esc、对话区新的滚轮手势或窗口尺寸变化关闭此临时菜单；程序化滚动与残余惯性 `scroll` 不得把刚打开的菜单立即关闭。点外关闭消费这一次指针输入，不把关闭菜单的点击传给底层对话。打开“本次回答信息”后才进入原有详情 Dialog。
- Composer 的模型选择仍保持已有底部 sheet、关闭按钮与点外关闭语义，但其 dismiss 层必须透明，不压暗对话背景；模型目录、选择和 Provider 真相链均不因视觉调整变化。

## 当前增量：附件卡片整卡反馈

- 搜索附件卡片以整卡承载主题色浅底高亮，预览、名称、摘要、文件信息与三点区域保持同一表面；内部按钮不再出现局部灰色底。预览按钮命中范围延伸到卡片，三点菜单维持独立上层命中；30px 按钮去除默认内边距，18px 图标水平／垂直居中，键盘聚焦保留可见反馈。

## 当前增量：搜索结果高亮

- 搜索文字卡片以轻阴影区分表面，不用明显线框；悬停、键盘聚焦采用 10% 当前主题色混合浅底，标题同步主题色，按下加深至 16%。沿用原有打开结果行为，不新增持久选中状态。

## 当前增量：模型服务切换尺寸

- 服务商分段卡居中占可用宽度 75%，窄窗口下以不超出容器的 320px 下限保护标签；轨道最小高度 40px、选项 34px、基础字号 11px（继续跟随应用字号缩放），选中背景与点击轮廓一致。

## 当前增量：DeepSeek V4.1 Flash

- 当前设置、日常候选、Composer 和新调用显示新版；实际 `deepseek-flash`、稳定选择键与旧选择兼容由 Rust 模型目录／路由 owner 管理。图片原文透过私有资产校验后进入 Chat Completions 或 Responses；Pro 不继承新版 Flash 的视觉能力。
- 双端当前模型规范见 [Android 会话合同](ANDROID_CONVERSATION_UI_CURRENT_CONTRACT.md)，官方核验、历史兼容、费用平台差异与验收见 [V4.1 升级记录](review/20260910/DEEPSEEK_V41_UPGRADE.md)。

## 当前增量：输入高度与动作图标

- Composer 文本区由 `composer-size.mjs` 按真实 scrollHeight 测量，最小 36px、上限 190px，超限内部滚动；输入、清空与恢复草稿共用该 owner，底部按钮保持独立一行。
- 收藏／取消收藏使用 Bookmark／BookmarkBorder，同步使用 CloudUpload，置顶与取消置顶共用 PushPin；20 个共享动作来自 AndroidX Material Rounded 原始路径，生成入口为 `sync-android-action-icons.py`。未转换图标仍须逐项核对，不能声称全量一致。

## 1. 最终视觉来源

- 用户确认来源：本轮提供的 ChatGPT Desktop 主对话、侧栏底部账号菜单，以及 ChatGPT/Codex 产品切换菜单截图。
- 采用的是成熟的信息架构：轻量侧栏、会话为第一入口、中央单一对话画布、固定输入区、低频能力进入菜单。
- 不复制 ChatGPT/Claude 的品牌、Logo、订阅、邀请、宠物、具体文案或账号业务；南枫 AI 使用自己的图标、领域状态和安全边界。
- 排除来源：当前 P6-D 默认三栏知识工作台、工程 Inspector 首屏、历史开发探针、静态 fixture 作为默认首页。
- 视觉冲突优先级：用户最新明确方向 > 南枫 AI 领域与安全事实 > 本合同 > 既有 P6-D 布局。

## 2. 第一用户任务与入口矩阵

第一用户任务是：打开 Desktop 后立即开始或继续一次对话；空白／新会话也必须能先选择 Auto 或具体模型，点击“发送”时把新会话、模型选择、user message 与 Attempt 作为同一 Rust SQLite 事务提交。Provider 未启用或凭据不可用时保留明确失败 Attempt，不把“模型可选择”误写成“Provider 已配置”。用户可随时切到工作模式管理本地资产，但不会离开同一浅色壳或失去返回对话的路径。

FB-P6-025 进一步锁定：本壳不是工程模块入口的视觉重排，而是意图优先入口。空态的画布与唯一 Composer、对话态的按需附件/来源/metadata/回底、drawer 的 search/pinned/recent 与底部 Settings、以及 USER/Assistant 的角色投影，必须分别证明意图进入、渐进揭示、持久治理和消息语义；详见 `CHAT_FIRST_INTENT_ORGANIZATION_CONTRACT.md`。

### 2.1 统一外壳的权威解释（2026-08-13）

- `对话 / 工作` 是真实模式切换，始终位于同一顶部栏；不是装饰标签，也不启动第二套页面架构。
- 对话模式左侧依次承载品牌、搜索、定时任务、南枫转写、会话历史，并在底部保留彼此独立的设置与新对话浮动控件；右侧承载当前对话与输入区。
- 工作模式进入、再次点击“工作”或切换工作区后，右侧默认必须是该工作区最近一次对话；没有会话时显示该工作区的新对话空态和同一输入区。工作区是当前对话的 scope，不得把 work-home、资产仪表盘或管理首页当默认右侧内容。
- 工作模式在同一左侧区域切换为工作区、项目、知识、记忆和本地受控记录；只有明确点击项目、知识、记忆或记录时，才替换右侧对话为对应二级管理页。知识与记忆是独立入口，不能在一个固定混合清单里同时铺开。
- 两种模式共用浅色侧栏、顶部栏、账号/设置入口、尺寸、token、焦点和明确返回路径；禁止加载或暴露深绿色 legacy workbench、固定 Inspector 或第二套导航。
- 输入区主动作固定为“发送”，本地消息、会话与草稿默认自动保留。空白／新会话的模型选择先作为本机 draft 状态保存，第一次发送时由 `submit_desktop_ordinary_chat` 将会话、精确模型、user message 与 Attempt 原子提交；已有会话的模型选择继续由会话 override owner 持久化。Provider 未配置时不读取 Key、不构造 Authorization、不请求网络，并保留可恢复的明确失败事实。

### 2.2 Composer 模型选择合同（2026-09-02）

- 模型选择入口与 Provider／API Key 就绪状态是两个独立事实。只要本地产品模型目录存在，空白／新会话必须显示实际 Auto 模型短名并保持入口可点击，禁止再以“未配置”替代模型目录。
- 根层只显示“自动／日常／深度”；日常与深度进入下一层完整候选列表。分组、候选顺序、全名与 Composer 短名跟随 Android 当前模型选择合同。
- 具体模型行的小字必须逐项读取当前 Android 模型目录中该 preset 的用户说明；不得显示 Provider ID、内部 family、路由键或其他工程标识。日常／深度二级页标题始终居中，标题下不重复显示分组泛化说明。
- P6-G 的 `LOCAL`／fixture 候选只属于自动化验收，不能进入用户可见候选、不能由 UI 安装，也不能以历史快照残留的形式漏进 Composer。
- Auto 不写精确模型 override；手动选择必须保存精确 preset ID。新会话在首次发送前允许修改选择，发送成功提交后清除 draft；发送前失败则保留选择和草稿供用户重试。
- 选择模型不代表 Provider、Key 或网络已经可用。真正发送时仍由模型服务 owner 做启用状态、凭据、外发与费用边界检查，禁止静默换模型或伪造成功。

| 概念 | 唯一所有者 | 主入口 | 二级入口 | 禁止项 |
| --- | --- | --- | --- | --- |
| 当前对话与消息 | Rust Desktop Conversation Domain | 左侧会话列表、中央画布 | 工作模式中的项目归属 | 首页同时展示 Knowledge/Memory/Inspector |
| 输入草稿 | Desktop Chat Shell 内存状态，未来接持久草稿合同 | 空态与内容态共用固定底部输入区 | 无 | 状态变化移动输入区、把空输入写入 SQLite |
| 本地/联网模型路径 | 双路径能力状态 owner | 输入区路径选择、设置 | 调用记录 | 把未配置写成“离线产品终态”或假装联网成功 |
| Project/Knowledge/Memory/relation | 既有 Rust Domain | 顶部“工作”模式 | 对话内按需引用 | 在默认聊天首屏铺开完整工作台 |
| 模型、账号、同步、本地数据 | 各自既有领域 owner | 侧栏底部设置 | 专用设置页 | 在首页堆状态卡或让一个总开关扩大外发范围 |
| Agent/P8/生态审计 | 既有只读 ledger / 合同 owner | 高级设置或工作模式 | 详情 | 作为一级导航抢占普通用户视线 |

## 3. 目标视口

| 形态 | 平台 | 类别 | 导航 | 页面结构 | 滚动所有者 |
| --- | --- | --- | --- | --- | --- |
| 唯一产品基准 | macOS/Windows 常规宽屏 | `1440×900` | 240–272px 常驻侧栏 | 侧栏 + 单一主画布 | 会话列表与消息列表各自滚动 |
| 防溢出验证 | macOS/Windows 较小窗口 | `1000×800`、`700×900` | 必要时收起为可恢复 drawer | 与宽屏相同的信息架构 | 消息列表 |

`1440×900` 是唯一产品视觉基准；较小窗口只验证同一信息架构没有溢出，不构成紧凑版、第二套产品层级或新的对外截图基准。宽屏仍一次只显示一个一级模式。既有 Inspector 不再作为统一 shell 的并排或默认入口；安全状态通过当前工作功能页和设置的最小必要信息呈现。

紧凑侧栏的关闭控件不是常驻导航：常规宽度完全不渲染/显示；仅在 `< 900px` 且 drawer 已打开时，在侧栏右上角显示小型 `×` 图标按钮，保留 `aria-label="关闭导航"` 而不显示“关闭”文字。点击它只关闭 `sidebarOpen`，不得改变当前对话/工作模式、workspace、conversation 或正在查看的二级页。

## 4. 视觉令牌

- 页面与主画布：`#FFFFFF`；侧栏：低饱和浅灰 `#F5F6F4`。
- 普通浮层/菜单：`#FFFFFF`；描边：`#E3E5E2`；不使用彩色模块卡片。
- 文字：`#202522`；辅助文字：`#6C746F`。
- 主结构/选中：南枫绿 `#176B52`，只用于小面积状态与关键动作。
- 联网等待/注意：橙色；错误：红色；不得用颜色替代文字状态。
- 圆角：侧栏功能行约 9–12px、搜索与底部动作使用胶囊形，Composer 外层 28px、普通浮层 14–18px。
- 阴影仅用于输入容器和浮层；主页面不套大型卡片或多层边框。

## 5. 页面空间顺序

### 聊天模式（默认）

- 侧栏：品牌 → 搜索 → 定时任务 → 南枫转写 → 分组会话列表 → 底部设置胶囊／新对话胶囊。底部只绘制两个独立浮动表面，不得加硬边白色托盘或紫色状态胶囊。
- 主区：轻量顶部栏 → 安静空画布或当前消息列表 → 固定底部 Composer。
- 空态不显示 Hero 图形、欢迎标题或持久化实现说明；空态与内容态共用同一底部 Composer。进入已有会话只切换顶部动作与消息滚动 owner，不移动 Composer。
- 消息溢出只滚动消息列表，侧栏会话列表独立滚动。

### 工作模式（工作区范围内的对话优先）

- 通过顶部“对话 / 工作”明确切换；两个切换在任何工作功能页始终可见可用。两种模式的默认右侧内容都是对话，不存在工作模式 dashboard 首页。
- 左侧按当前模式显示对应功能集；工作区切换当前 Conversation scope，并立即选中该 scope 最近会话；无会话时保留新对话空态。项目、知识、记忆和本地受控记录都在同一浅色 shell 的右侧内容面内按需运行。
- 可复用既有 Project/Knowledge/Memory/relation、撤销/重做、导入导出与只读 P8 能力，但不保留其深色三栏/Inspector 外壳。

### 设置与账号

- 由侧栏底部菜单进入。
- 模型 Provider、API Key 存在性、目录新鲜度、逐次外发确认、账号加密同步分别展示，不合并成一个“联网”总开关。
- 未配置时统一显示“联网未配置，本地可用”；不得显示假账号、假服务成功或假费用。

## 6. 状态矩阵

| 状态 | 用户可见文案 | 操作 | 持久化/恢复 |
| --- | --- | --- | --- |
| 本地发送 | 原子记录会话、模型选择、user 与 Attempt | 新会话可先选 Auto／具体模型再发送 | Rust SQLite transaction/revision |
| Provider 未就绪 | 模型仍可选择；本地记录保留，回答明确失败 | 打开模型设置或改选已启用服务 | 不读取缺失 Key、不发 HTTP、不静默换模型 |
| 已配置待确认 | 将发送给明确 Provider/Model，费用可能产生 | 逐次确认 | Consent 只绑定本次请求 |
| 联网运行 | 正在等待/生成，停止可用 | 停止 | 由 Invocation/Event owner 持久化 |
| 联网失败 | 中文原因与下一步，本地内容保留 | 重试、换模型或继续本地 | 新 Invocation，不覆盖旧事实 |
| 同步未配置/冲突 | 本地真值可用；同步未配置/需处理 | 账号与同步设置 | P7 状态机与账本 |

## 7. 交互与动效

- 聊天/工作和会话切换即时完成，无整页淡入或滑动。
- 空态、已有会话和普通状态切换均保持同一底部 Composer 锚点；焦点态只在 Composer 内展开编辑区与底部动作行，不移动其页面级 owner。
- 回车发送、Shift+Enter 换行；空白拒绝。失败或取消保留输入和已有消息。
- 侧栏底部设置胶囊进入设置页；不得再出现第二个账号菜单或遮挡全屏的工程 Dialog。
- 低频项目、知识、同步、审计不增加一级导航；通过工作模式或设置进入。

## 8. 验收矩阵

- [x] 展开态默认聊天空态、已有会话结构、设置状态。
- [x] macOS 紧凑态侧栏开关、显式关闭与 Escape 恢复；主区保持固定输入和同一浅色壳。
- [x] 紧凑关闭按钮：17 项 Node 合同及最新 macOS `.app` 已验证常规壳不渲染该按钮；打开 drawer 时只有右上 `×` 与 `aria-label="关闭导航"`，关闭不改变 scope、会话或当前二级页。
- [x] 紧凑态消息长列表滚动与窗口恢复：`P6D_COMPACT_CHAT_SCROLL_AND_WINDOW_RECOVERY_CONTRACT.md` 的 64 条隔离合成会话已经正式 UI 导入，并在最新 macOS `.app` 实际滚至第 19–23 条；固定输入区、drawer close、窄→宽自动关闭与再窄不残留 drawer 均已可见验证。
- [x] 本地发送/追加消息 typed command、空白拒绝、自动草稿本机保存合同；真实 webview 指针与重启读回仍见 evidence 限制。
- [x] 工作区范围对话优先：17 项 Node 合同与最新 macOS `.app` 已证明 `工作`、工作区切换和无会话分支不再路由到 `work-home`；隔离 workspace 的“工作”默认直接显示最近会话与同一输入区。
- [x] 联网未配置、本地可用、目录/Key/Consent/同步状态不误报。
- [x] 空白／新会话显示实际 Auto 短名、模型按钮可点击；Auto／日常／深度与日常完整候选可见，手动选择在首次发送事务内落库。
- [x] 前端测试、Rust 测试、Lint/typecheck/build、Tauri `.app` 构建。
- [x] 实际最新 `.app` 截图与参考结构同屏对比；视口比例差异已记录。
- [x] 2026-09-03 C-01～C-03：当前 Android 隔离模拟器与 Desktop `1440×900` Browser 已分别采集空态、内容态、侧栏截图及语义树；Browser console warning/error 为 0，页面溢出为 0。
- [x] 2026-09-03 隔离 Tauri：全新临时数据库已实看 C-01 空画布与 C-03 空列表侧栏；正式 Desktop 数据根未读写。

## 9. 未验证范围

- 真实 OpenRouter 流式、Token/费用与 Provider 失败恢复仍需后续真实服务合同；本轮不读取已保存 Key、不发真实 HTTP。
- 隔离 Tauri 的 C-02 内容会话仍未验收：全新临时根没有工作区／本地模型目录，Composer 发送按合同禁用；不得用 Browser fixture 或 Android 失败卡替代原生内容态。
- 真实 Google/Supabase、跨设备恢复、Windows 原生安装包、Developer ID/notarization 与真机账号仍是独立证据。
- 用户截图是结构方向，不是同分辨率像素稿；最终视觉以最新实际 `.app` 的清晰度、密度与用户复核为准。

### 全局表面层级与输入边界（2026-09-10）

- 页面局部控件与正文位于基础层；侧栏 20、分割线 25、搜索页／侧栏底部 40、上下文菜单及展开的 Composer 100、设置选择器 400、全局 scrim 1000。局部数值只在各自 stacking context 内比较。
- 全局预览／弹窗必须覆盖侧栏、分割线和底部操作。弹窗出现时关闭背景菜单，并将背景分支设为 inert；关闭恢复原始 inert 和入口焦点，Tab 与 Escape 在最高层处理。
- 生命周期及记忆引用菜单进入浏览器 top layer，保持原 DOM 动作 owner，避免被父滚动容器裁切；不得以继续堆高子元素 z-index 代替消除裁切。
- 预览尺寸和格式合同统一见 [本地内容预览合同](P6F2_UNIFIED_SEARCH_HISTORY_AND_LOCAL_CONTENT_PREVIEW_CONTRACT.md#当前-desktop-预览与层级2026-09-10)，本轮分层验收见 [记录](review/20260910/PREVIEW_LAYER_FIXES.md)。

### 会话行末端按钮反馈（2026-09-10）

- 整行灰底继续覆盖右侧操作区；置顶、归档和更多按钮各自独立悬停／键盘聚焦点亮，使用当前主题浅底及主题图标色，按下进一步加深。正常态透明，不让另外两个按钮跟着点亮。
- 反馈复用原按钮轮廓，保持点击动作、顺序和命中范围。浅色／深色与每个按钮分别验证；证据见当前交接。

### 浅色侧栏底色（2026-09-10）

- 浅色模式左侧导航统一为中性浅灰 `#F5F5F5`，与右侧 `#FFFFFF` 内容面轻微区分；颜色由 `desktop-theme-owner.mjs` 的 drawerBase 提供，初始 CSS 同步，深色模式沿用既有令牌。

### 设置灰底与记忆摘要（2026-09-10）

- Desktop 浅色设置背景为 `#FAFAFA`（Android 按用户要求保持 `#EDEDED`）；设置内部中性灰卡／路径底等按与白色差值保留 30% 处理，深色、语义色和主按钮／选中态保留。
- 记忆摘要取消“询问或更新”输入框、提交选择弹窗及旧底部占位；保留右上角编辑、刷新、删除、关闭与关于入口。

### 服务商切换可读性（2026-09-10）

- 服务商切换条由 75% 回调到 85% 宽度，轨道 40→46px、按钮 34→40px；四个名称由 11→14px 并统一 700 字重，沿用现有配色和胶囊轮廓。

### 设置输入卡片阴影（2026-09-10）

- 直接承载输入的设置 input／textarea 与独立白卡复用 `--settings-card-shadow: 0 2px 8px rgb(22 28 24 / 6%)`，沿用已有圆角。已有外层阴影的密钥卡，其内嵌输入保持无独立阴影，避免重复叠加；其他平台不由 Desktop CSS 变更推导同步修改。
